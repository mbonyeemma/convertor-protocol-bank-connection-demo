# DFC Bank Demo - Minimal Environment Variables
# Copy to .env and fill in your values

# Database (REQUIRED)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=bankdemo

# Keys (REQUIRED - paste in Settings UI instead)
# BANK_PRIVATE_KEY_PATH=./keys/bank_private.pem
# CONVERTOR_PUBLIC_KEY_PATH=./keys/convertor_public.pem

# Optional
NODE_ENV=development
PORT=5001

# Note: BANK_CODE, BANK_NAME, CONVERTOR_API_URL, and keys
# can be set via Settings page in admin UI (no need for .env)
